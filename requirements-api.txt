# ─── Collat.AI — API-only (inference) dependencies ─────────────
# Pre-trained models in repo `models/` — no training, Streamlit,
# scrapers, or PyTorch image stack.
#
#   py -3.12 -m venv venv
#   .\venv\Scripts\Activate.ps1
#   pip install -U pip
#   pip install -r requirements-api.txt
#
# Full stack: requirements.txt
# ───────────────────────────────────────────────────────────────

fastapi>=0.109.2,<1
uvicorn[standard]>=0.27.0,<1
pydantic>=2.6.0,<3
python-multipart>=0.0.9,<1

numpy>=1.26.4,<3
pandas>=2.2.0,<3
scipy>=1.12.0,<2
scikit-learn>=1.4.0,<2
joblib>=1.3.2,<2
xgboost>=2.0.3,<3
lightgbm>=4.2.0,<5
shap>=0.44.0,<0.50
